<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BattleMapMain.Views.SessionView"
             xmlns:Models="clr-namespace:BattleMapMain.Models"
xmlns:viewmodels="clr-namespace:BattleMapMain.ViewModels"
             Title="SessionView"
             x:DataType="vie​​​​wmodels:SessionViewModel"
             >
    <ScrollView>
        <VerticalStackLayout>
            <CollectionView
    x:Name="collec​​​tion"
    Grid.Row="1"
    Grid.Column="0"
    Grid.ColumnSpan="3"
    Margin="3"
    ItemsSource="{Binding UsersI​​​​​​​​​​​​​​​​​​​​nSession}"
>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="Models:User">
                        <Border
                    Margin="4,5"
                    Padding="4"
                    HeightRequest="80">
                                <Grid ColumnDefinitions="*,*,*,*">
                                    <Label
                            Grid.Column="0"
                            Margin="3"
                            VerticalTextAlignment="Center">
                                        <Label.FormattedText>
                                            <FormattedString>
                                            <Span FontAttributes="Bold" Text="{Binding U​​​serName}" />
                                                <!--<Span Text="{Binding ProfileName}" />-->
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                </Grid>
                            </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <Button 
Text="not sessin"
Command="{Binding SessionCom​​​​​​​​​​​​​​​​​mand}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>